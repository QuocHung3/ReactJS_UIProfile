import { clsx } from 'clsx'

import styles from './News.module.scss'

const News = () => {
    return(
        <div className={clsx(styles.main_container)}>
            News page
        </div>
    )
}

export default News